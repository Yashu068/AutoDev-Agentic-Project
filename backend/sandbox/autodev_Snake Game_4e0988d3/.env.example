# Example environment variables for the Snake Game
# These values can be overridden in a .env file

# Game window width in pixels
GAME_WIDTH=800

# Game window height in pixels
GAME_HEIGHT=600

# Size of each block (snake segment, food) in pixels
GAME_BLOCK_SIZE=20

# Frames per second (game speed)
GAME_FPS=10